# Archived Offline Route-Prior / RARP Evidence

Phase AC-1B-1 classifies this directory as archived, offline, non-mainline
evidence.

- Not Agent Catalog v2 acceptance evidence.
- Not default quality-gate evidence.
- Not current routing-quality promotion evidence.
- Retained for historical lineage and offline reproducibility only.
- Runtime mainline does not import these offline fixtures or generated outputs.
- Do not use these files as proof that current Router behavior or public
  workflow behavior has changed.

The runtime route-prior source files under `src/react_agent/` remain in place as
default-disabled, fail-open shadow code because `react_agent.graph` still imports
them.
