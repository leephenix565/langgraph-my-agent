# 报告完善计划（中文）

日期：2026-06-17

范围：主系统 fixed DAG 报告链路，以及后续 sandbox 中真实外部 agent wrapper 的报告材料增强。

本计划不改变 `runtime_bindings.json`，不设置 `live_verified=true`，不设置
`invoke_enabled_by_default=true`，不把 `/compute` 证据当成 `/invoke` 证据，不把
default-off demo bridge 当成生产默认路径，也不把 placeholder agent 当成真实完成。

## 核心判断

最终中文报告像模板，根因不是单纯 prompt 不够好，而是进入报告链路的 agent 研究材料太薄。
报告生成器只能读到 summary、stance、confidence、少量 evidence 时，LLM 或 fallback 模板都会
自然变成固定报告格式。

第一阶段采用保守方案：不新增大而全外部协议，先把已经存在于 `agent_conclusion_v1` /
`dimension_conclusion_v1` / `risk_conclusion_v1` / `macro_conclusion_v1` 中的 public-safe
材料投影到报告输入包。

## 报告材料约定

主系统优先读取四类材料：

1. `domain_metrics`
   领域核心指标，例如估值桥、合理价值区间、模型票数、风险分、宏观 regime、成员数量。

2. `drivers`
   结论驱动材料，例如方法假设、模型投票表、top features、rubric 表、成员权重、降权原因。

3. `data_quality`
   可信度边界，例如数据时点、缓存、覆盖率、缺失组件、反前视、合成语料声明。

4. `research_points`
   agent 自己基于确定性材料生成的可报告化研究判断：
   `claim`、`support`、`interpretation`、`decision_implication`、`caveat`。

这些字段必须 bounded、public-safe，并且不能包含 endpoint、secret、raw provider response、
raw external JSON、traceback 或内部推理草稿。

## L3 与 LLM 边界

L3 composite 的核心融合不交给 LLM。

- 确定性融合核心负责 `stance`、`gate`、`risk_score`、`dimension_weights`、member weights、
  confidence 和 status。
- LLM 只能作为默认关闭的语言解释/冲突归纳层，读取 public-safe L2/L3 材料后生成
  commentary 或 `research_points`。
- LLM 解释层不能覆盖 deterministic fusion 数值，不能把 placeholder/partial 成员包装成真实
  complete evidence。

因此后续 L3 改造方向是：先让 deterministic L3 输出成员权重、冲突、降权和边界；如需 LLM，
只用于解释这些已确定结果。

## 阶段计划

### Phase 1：主系统报告材料层

目标：

- adapter 投影 `domain_metrics`、`drivers`、`data_quality`、`research_points`。
- `report_input_bundle_v1.agent_evidence_bundle` 保留 L2/L3 详细材料。
- fallback 报告优先展开 `research_points`，再显示 evidence、metrics、drivers、quality。
- fallback 报告只展开有 public-safe 材料或 error 状态的 L2；无可读证据的 placeholder/pending
  L2 保留在 `report_input_bundle_v1` 和 workflow trace 中，但正文只汇总缺失覆盖，不逐条当作
  研究材料渲染。
- L3 quality summary 区分 complete、partial、error、available。
- demo bridge 将风险 L2 `provenance.risk_score` 安全透传给 `risk_composite` upstream。
- LLM report synthesis 增加 secret-free provider preflight。
- graph/smoke runner 支持显式 `fixed_dag_as_of`。

验收：

- 主系统相关单测通过。
- 不改 runtime binding 和 live flags。
- 不调用 endpoint。

### Phase 2：前三个真实 L2 agent wrapper

只处理已审计且已有真实材料的 sandbox 服务副本：

- `value_traditional_valuation`
- `market_stock_technical`
- `risk_compliance_review`

目标是 wrapper 输出可追溯研究材料，不重写模型：

- 估值：估值桥、方法假设、置信度因素、行情/财报时点、估值判断。
- 技术：模型投票、校准概率、top features、数据完整度、短线判断。
- 合规：十维 rubric、最低维度、主要发现、top terms、合成语料声明。

执行前必须比较 prod 和 sandbox 对应 agent 文件，避免覆盖其他开发者刚同步到 prod 的修改。

当前进度（2026-06-17）：

- 已比较 prod 与 sandbox 三个服务副本，三者均不一致。
- `market_stock_technical` 与 `risk_compliance_review` 的差异主要是 sandbox 在 prod 基础上增加
  `research_points`、`drivers`、`data_quality` 和更厚 evidence，未发现需要先从 prod 回灌的
  业务逻辑差异。
- `value_traditional_valuation` 的 prod 副本已有 fixed DAG direction 所需的 top-level
  `stance` / `confidence` 投影、`assumptions`、`method_details` 以及关键财务/估值假设/
  方法选择证据句；sandbox 的报告材料实验曾遗漏这些 prod-only 字段。
  已在 sandbox 服务副本中合并保留这些字段，并补回对应回归测试断言。
- 已在独立临时 venv `/tmp/lma-service-test-venv` 中安装服务测试依赖，避免污染主系统
  `.venv`。
- 已做静态语法检查：三个 sandbox 服务源码和传统估值新增回归测试可编译。
- 已通过服务本地契约测试：
  - 传统估值：`tests/test_domain_contract_v1.py`，7 passed。
  - 技术分析：`tests/test_domain_contract_v1.py -k 'not invoke'`，9 passed，1 deselected；
    deselected 用例是 `/v1/agent/invoke`。
  - 公告合规：`announcement_compliance/agent/tests/test_domain_contract.py`，11 passed。
- 这些测试使用进程内 FastAPI TestClient 和离线 fixture，不启动外部服务栈，不调用外部
  `/v1/agent/invoke`。

仍未做：

- 未修改 prod 服务副本。
- 未调用 live runtime `/health`、`/compute`、`/invoke`；技术分析 `/invoke` 本地测试也显式跳过。
- 未把 sandbox demo bridge 作为生产默认路径。
- 未把 placeholder agent 纳入真实 evidence。

### Phase 3：controlled sandbox demo

只在显式确认后运行：

- 使用 default-off demo bridge。
- 只 allowlist 已确认真实 agent。
- 只调用 `/compute`，不调用 `/invoke`。
- 输出完整 `agent_evidence_bundle_v1`、每个真实 agent 的材料、最终中文报告和 workflow trace。

验收不看字数，而看材料质量：

- 每个真实 L2 是否有可读 `research_points`。
- metrics / drivers / quality / evidence 是否进入 report bundle。
- L3 是否显示成员权重、冲突、降权和 partial 影响。
- 最终报告是否引用 agent 判断，而不是只复述模板。

当前进度（2026-06-17）：

- 已运行 controlled sandbox compute demo，artifact：
  `/tmp/lma-phase3-sandbox-compute-demo/20260617T1510Z`。
- 主系统使用 dev 当前 fixed DAG 代码；三个外部 L2 使用 sandbox service 副本的
  FastAPI TestClient `/v1/agent/compute` handler，不占用或重启 prod 监听端口。
- allowlist 仅包含：
  `value_traditional_valuation`、`market_stock_technical`、`risk_compliance_review`。
- 三个真实 L2 均 mapped 成功，failed agents 为空。
- `agent_evidence_bundle_v1` 中三家真实 L2 均包含
  `research_points`、`domain_metrics`、`drivers`、`data_quality`、`evidence_items`。
- 报告状态为 `partial`：L2 complete 3/18，partial 15；L3 available 0/4。
- 最终中文报告为 provider-missing fallback 报告，未调用外部 LLM provider；它已经引用三家
 真实 L2 的研究判断和证据，但语言组织仍偏固定模板。

本轮输出要点：

- 传统估值：PE 法给出合理市值中枢 8612.0706 亿元、当前市值 2121.0669 亿元，
  折溢价约 +306.0%；同时保留行情日、财报期、公告日、方法假设和置信度边界。
- 技术分析：xgb、catboost、ensemble 三票均偏上涨，综合方向信度 54.16%，top features
  包括 close、LOG_PRICE、MA5、MA10、EMA12；边界是短周期量价信号，不代表长期价值。
- 公告合规：十维 Rubric 综合得分 94.44/100，最低维度为准确性 88.0/100；
  同时明确当前为本地演示/合成公告语料，不得包装成真实交易所公告结论。

仍未做：

- 未调用外部 `/v1/agent/invoke`。
- 未修改 runtime binding 或 live flags。
- 未把 15 个 placeholder/partial L2 算作真实 evidence。
- 未证明 L3 composite 的 service-owned research material；四个 L3 仍是
  `not_evaluated` / `pending_implementation`。
- 未验证真实 LLM provider 生成的自然语言报告。

### Phase 4：L3 composite 解释层

四个 L3 不改融合算法，只增强解释材料：

- `value_composite`：估值分歧、成员权重、fair-value range、主导因子。
- `market_composite`：技术/资金/舆情成员信号、降权原因、短中期冲突。
- `risk_composite`：gate、risk_score、成员风险贡献、partial 成员影响。
- `macro_composite`：regime、value/market 调权依据、缺失宏观成员影响。

LLM 只能作为默认关闭的 language explanation / conflict summary 层，不能成为融合算法。

当前进度（2026-06-17）：

- 主系统 deterministic L3 builder 已能从本轮已有 L2 输出生成 bounded report material。
- `value_composite` / `market_composite`：
  - 只对 `complete` / `partial` 且具备可读方向的成员赋权。
  - 权重按成员 confidence 归一化；缺失、pending、placeholder 成员权重为 0。
  - L3 confidence 会按成员覆盖率折扣，避免一个真实 L2 被误读成完整维度综合。
- `risk_composite`：
  - 只读取风险维 L2，不读取 `sentiment_company_radar`。
  - 基于可用成员 `risk_score` 形成 `pass` / `manual_review` / `veto` gate。
  - 成员缺失或缺少 `risk_score` 时记录覆盖率和边界，不把缺失当成低风险证明。
- `macro_composite`：
  - 没有可用宏观成员时仍保持 `pending_implementation` / `not_evaluated`。
  - `value` / `market` 权重保持 0.5 / 0.5，不在宏观证据不足时主动调权。
- L3 provenance 会输出 `member_weight_summary`、`domain_metrics`、`drivers`、
  `research_points`、`data_quality`，并进入 `agent_evidence_bundle_v1` 和 fallback
  中文报告。
- `risk_composite` 额外保留成员级 `member_boundary_summary`：把风险 L2 的 warnings、
  模型版本边界、缺失文本等 caveat 提升到 L3 `drivers`、`research_points` 和
  `data_quality`。这不改 `risk_score`、`gate` 或成员权重，只避免 L3 压缩后最终报告看不见
  成员边界。

当前进度（2026-06-18）：

- 主系统已新增默认关闭的 L3 LLM 解释层：
  `src/react_agent/fixed_dag_l3_explanation_synthesizer.py`。
- 开关为 `Context.enable_llm_l3_explanation` / `ENABLE_LLM_L3_EXPLANATION=1`；
  可选模型覆盖为 `Context.llm_l3_explanation_model` /
  `LLM_L3_EXPLANATION_MODEL`。
- 解释层只读取 public-safe L2 conclusions 和 deterministic L3 composite results。
- 成功时只向 L3 provenance 增加 bounded `research_points` 和
  `provenance.llm_explanation`，这些材料会进入
  `report_input_bundle_v1.agent_evidence_bundle.l3_composite_outputs[]`。
- 解释层会保留 deterministic L3 的 `stance`、`confidence`、`status`、`gate`、
  `risk_score`、`dimension_weights`、member weights 和 contributing agents。
- 缺 provider 配置、模型加载失败、非 JSON、unsafe output、schema 不合规或解释后 L3
  校验失败时，回退到原始 deterministic L3 结果。
- 已用 fake model 单测验证解释材料能进入 L3 和 report input bundle，且不会覆盖融合字段。
- 已做真实 provider 验证，artifact：
  `/tmp/lma-r8-13n-l3-real-provider-demo/20260618T061151Z`。
  - L3 explanation：attempted=true，provider_invoked=true，used_llm_explanation=true。
  - final report synthesis：attempted=true，provider_invoked=true，used_llm_report=true。
  - `report_input_bundle_v1` 和 `report_result_v1` 均通过校验。
  - 验证只使用本地 public-safe fixture L2 材料，不调用外部 agent endpoint。

本轮 6-agent 直接产物的 L3 验证（2026-06-18）：

- 复用 6-agent direct sandbox L2 artifact：
  `/tmp/lma-6agent-direct-bundle-check/20260618T073557Z`。
- 通过主系统 deterministic L3 builder 生成 L3，不调用外部 L3 服务：
  artifact `/tmp/lma-6agent-l3-llm-report-synthesis/20260618T075647Z`。
- 生成并校验通过 3 个 L3：
  - `value_composite`：partial，stance=neutral，confidence=0.6104，成员数 3；
    partial 的直接原因是 `value_ml_valuation` 财务快照缺失。
  - `risk_composite`：complete，gate=pass，risk_score=0.0949，confidence=0.76，
    成员数 2。
  - `macro_composite`：complete，regime=neutral，confidence=0.94，成员数 1。
- `agent_evidence_bundle_v1.quality_summary`：L2 complete 5/6，L2 partial 1/6；
  L3 complete 2/3，L3 partial 1/3，L3 available 3。
- 再次运行 default-off 真实 LLM report synthesis，`deepseek/deepseek-chat`
  被显式调用；输出 `report_result_v1` 校验通过，最终报告不再把 L3 说成缺失。
- 这次验证暴露的下一批真实质量缺口：
  - `value_ml_valuation` 因缺少财务指标快照仍为 partial，是价值维度当前最大数据缺口。
  - `risk_identification` 的业务数据日期早于统一 `as_of=2024-12-31`，报告已能提示时点差异，
    但后续应审计其数据源和 as-of 选择。
  - 当前仍是直接 mapped L2 artifact + 主系统 deterministic L3，不是外部 L3 服务 live
    readiness，也不是 `/invoke` evidence。
- 后续已补 `risk_composite` 成员边界汇总，并重建 6-agent 报告：
  `/tmp/lma-6agent-risk-l3-boundary-l3-llm/20260618T084040Z`。
  - `risk_crash`：`risk_score=0.2079`、`confidence=0.62`，
    `feature_data_anti_lookahead_passed=true`，`model_vintage_caveat=true`。
  - `risk_composite`：`gate=pass`、`risk_score=0.1036`，同时在
    `member_boundary_summary` 中保留 `risk_crash` 的模型版本 caveat 和
    `risk_identification` 的 MD&A 缺失 caveat。
  - 最终 default-off LLM 报告校验通过：5 个 sections、9 个 evidence cards、7 条
    limitations；旧的“训练数据前视问题”表述消失，并明确写出风险模型版本边界。

后续修复进展（2026-06-18）：

- 已在 sandbox `value_ml_valuation` 中补本地财务快照兜底：
  - 新增可配置 `VALUATION_FINANCIAL_CACHE_DIR`，同时支持服务目录下 `_pe_train_cache`。
  - 只选择 `ann_date <= as_of` 的最新财务行，避免把 2025-04-03 才公告的 2024 年报用于
    2024-12-31 的判断。
  - 从本地 income cache 拼回 `rd_exp`，并把 `financial_report_period` /
    `financial_publish_time` 投影进 public-safe report material。
- 已补主系统 adapter 白名单，让 `financial_report_period` 和
  `financial_publish_time` 可进入 `domain_metrics` 与 `data_quality`。
- 使用只读 dev 外部 agent cache 路径作为 sandbox 进程环境重新验证：
  `/tmp/lma-6agent-ml-financial-cache-l3-llm/20260618T080604Z`。
  - `value_ml_valuation`：status=ok，financial_data_source=local_financial_cache，
    financial_report_period=20240930，financial_publish_time=20241026。
  - ML 估值中心 1813.801 元，90% 区间 1498.2501 至 2132.6964 元，隐含空间
    +19.0158%。
  - `agent_evidence_bundle_v1.quality_summary`：L2 complete 6/6，L3 complete 3/3。
  - 最终 default-off LLM 报告校验通过，不再把价值 L3 或 ML 估值标为 partial。
- 生产落地仍需要一个归属清晰的数据路径：把 cache 复制/挂载到 sandbox/prod 服务自己的目录，
  或改为读取正式 L1 data bundle / data service；当前验证没有修改 `.env`，也没有修改 prod。

进一步收敛（2026-06-18）：

- 已把 `value_ml_valuation` 的财务快照从临时 dev cache 依赖收敛成 sandbox 服务自有 CSV：
  `data/cache/financial/fina_indicator_*.csv` 与 `data/cache/financial/income_*.csv`。
- `spts_store.available()` 现在会把本地 cache 视为可用数据源；财务读取层支持 CSV 与 pkl。
- 已加入 600519.SH 的 2024Q3 与 2024 年报财务行。`as_of=2024-12-31` 会选择
  2024Q3，因为 2024 年报 `ann_date=2025-04-03`，不会发生前视。
- 已在不设置 `VALUATION_FINANCIAL_CACHE_DIR` 的情况下重跑：
  - `value_ml_valuation` 测试 24 passed。
  - direct compute 仍返回 status=ok、financial_data_source=local_financial_cache、
    financial_report_period=20240930、financial_publish_time=20241026。
- 最新完整 artifact：
  `/tmp/lma-6agent-service-owned-cache-l3-llm/20260618T082145Z`。
  - L2 complete 6/6，L3 complete 3/3。
  - 最终 default-off LLM 报告校验通过。
- 生产落地仍需把同样的服务自有 cache 或等价数据服务路径同步到 prod；本轮没有改 prod。

`risk_identification` 时点修复进展（2026-06-18）：

- 审计结论：代码的 as-of 选择逻辑本身正确，按 `ann_date <= as_of` 选择最新期；旧报告显示
  2024-04-02，是因为本地 `companies.json` 对 600519.SH 只包含到 2023 年报。
- 已在 sandbox `companies.json` 中为 600519.SH 增加 2024Q3 数值 period：
  - `report_period=2024-09-30`，`ann_date=2024-10-26`，`feature_year=2024`。
  - 行情/波动特征来自本地日线 parquet 和 `dbasic_20241231.pkl`。
  - 财务特征来自企业财务分析智能体本地 Tushare snapshot。
  - 沪深 300 beta 来自本地指数日线 parquet。
  - `mda_text` 明确留空，因为本地证据没有对应 MD&A 原文；agent 会输出 warning，
    文本 `_hit` 特征按 0 处理并降低 confidence。
- 已更新 as-of leakage 测试，`as_of=2024-12-31` 现在应选择 2024Q3 period。
- direct app handler + adapter 验证结果：
  - response agent_id=`risk_identification`，external_agent_id=`market_risk_reasoning`。
  - status=ok，data_as_of=2024-10-26，risk_score=0.019，confidence=0.765。
  - adapter 输出 `conclusion_object_v1` complete，4 条 research_points，9 个 drivers，8 条 evidence。
- 最新 6-agent bundle 与 LLM 报告 artifact：
  `/tmp/lma-6agent-ml-risk-current-l3-llm/20260618T081227Z`。
  - L2 complete 6/6，L3 complete 3/3。
  - risk_composite：gate=pass，risk_score=0.1036。
  - 最终 LLM 报告校验通过，并明确写出风险识别使用 2024Q3 财务数据、缺少 MD&A 文本。
- 生产落地仍需服务 owner 提供全市场、全 period 的正式快照更新管线；当前只是 600519.SH
  sandbox demo 的可追溯数值快照补强。

仍未做：

- 未调用外部 L3 服务或 `/v1/agent/invoke`。
- 未启用 runtime binding 或 live flags。
- 未把 placeholder L2/L3 当作真实完成。
- 未把真实 provider 调用设为默认路径；当前验证仍是显式 default-off demo。

当前进展（2026-06-19）：

- 主系统 default-off compute bridge 已加厚 L2 -> 外部 L3 的安全上游材料：
  `context.upstream_outputs` 现在可携带 bounded public-safe
  `domain_metrics`、`drivers`、`research_points`、`data_quality` 和 evidence；
  unsafe 字段如 endpoint、secret、raw response、traceback 会被过滤。
- `agent_task_v1.upstream_results` 同步保留上述 L2 研究材料，避免外部 L3 只能看到
  summary、stance、confidence。
- Adapter allowlist 已补外部 L3 research packet 字段：
  `composite_research_packet`、`composite_quality`、`conflict_summary`、
  `dominant_signals`、`missing_or_degraded_members`、`final_implication`、
  `limitations` 等。
- Sandbox 四个 L3 服务副本已补 deterministic service-owned research packet：
  - `value_composite`：输出估值成员共识、主导成员、成员冲突、覆盖率和 fair-value 边界。
  - `market_composite`：输出市场主导信号、方向冲突、低置信成员、partial/placeholder 边界。
  - `risk_composite`：保持原 `risk_score`/`gate` 加权逻辑不变，新增风险成员贡献、
    风险分散度、partial 成员和 D-S 富融合未在 wrapper 中重算的边界声明。
  - `macro_composite`：输出 regime、value/market 权重、风险敏感度、宏观成员覆盖和
    placeholder/partial 成员边界。
- 本轮没有改四个 L3 的融合算法，没有调用 LLM，没有调用 `/invoke`，没有修改 prod 服务副本。

### Phase 5：扩展更多真实 L2

优先级：

- P0：`value_research_synthesis`、`risk_crash`、`macro_index_valuation`。
- P1：`value_ml_valuation`、`value_meta_valuation`、`risk_identification`、
  `risk_financial_fraud`、`macro_analysis`、`macro_commodity_pricing`。

当前进度（2026-06-18，sandbox wrapper）：

- 已完成只读比对：`value_research_synthesis` 和 `macro_index_valuation` 的 sandbox
  服务副本已不同于 prod，差异主要是 sandbox 已有报告材料增强；`risk_crash` 的
  prod/sandbox 代码此前一致，仍只有很薄的 `raw_output` 和单条 evidence。
- `value_research_synthesis` sandbox wrapper 已能从 BTL/TOPSIS 分析师加权共识中输出
  `analyst_consensus_bridge`、`forecast_inputs`、`coverage_summary`、自然语言
  `research_points` 和多条 evidence。其 `/compute` 仍不调用 LLM；LLM 仅属于 `/invoke`
  解释层。
- `macro_index_valuation` sandbox wrapper 已能输出 `macro_direction_bridge`、
  `percentile_context`、`forward_return_context`、`data_window`、`method_assumptions`、
  `confidence_factors` 和自然语言 `research_points`。它仍是 owner 批准的 macro L2
  `agent_conclusion_v1 + role=direction`，不是 `macro_conclusion_v1` regulator。
- 本轮已补 `risk_crash` sandbox wrapper：不重训 Hist GBDT 模型，不新增 LLM，只把已有
  `risk_score`、横截面分位/排名、top features、历史 NCSKEW/DUVOL/CRASH 窗口、模型测试
  指标和 as_of 数据边界整理为 public-safe `drivers`、`research_points`、更厚 evidence
  和 `quality`。
- 已进一步精化 `risk_crash` 的模型版本边界：`model_vintage_boundary` 明确区分
  “输入特征/公告/历史标签按 as_of 截断”与“历史 as_of 使用当前生产模型版本的 caveat”。
  这不消除真实 caveat，也不把它降级为无风险；目标是避免最终报告把模型版本边界误写成
  本次特征数据晚于 as_of。
- 已补轻量回归测试，验证 `risk_crash` 的报告材料构造以及 fixed DAG `gate_member`
  投影不会丢失这些材料。
- 已审计 P1 第一批：`value_ml_valuation` 和 `value_meta_valuation` 的 sandbox wrapper
  已有报告材料增强，分别输出机器学习估值桥/蒙特卡洛假设/特征快照，以及同业支持集/
  方法假设/置信度边界。
- 本轮已补 `risk_identification` sandbox wrapper：不改 rule-margin/MD&A-hit logistic
  风险评分算法，不新增 LLM，只把已有三维风险概率、阈值、top drivers、MD&A hits、
  t+1 validation 和数据边界整理为 public-safe `drivers`、`research_points`、更厚
  evidence 和 `quality`。
- 已补 `value_ml_valuation` 和 `value_meta_valuation` sandbox wrapper 的 fixed-DAG
  顶层投影：把已有 `normalized.stance/confidence` 同步为顶层 `stance/confidence`，
  解决主系统 adapter 的 `direction_stance_missing`，不改估值模型、不改估值算法。
- 本轮已补 `risk_financial_fraud` sandbox wrapper：不改 HyFormer 财务造假分类器、不改
  `risk_score`/闸门阈值、不新增 LLM，只把已有模型概率、阈值、规则降级原因、年度特征可得日、
  特征覆盖率、风险标签和证据线索整理为 `fraud_risk_bridge`、`model_context`、
  `feature_diagnostics`、`risk_gate_rule`、自然语言 `research_points`、更厚 evidence
  和 `quality`。该 agent 的 `/compute` 仍是本地财务特征快照 + HyFormer + 规则 fallback；
  `/invoke` 的 DeepSeek 只属于新闻文本解释/分类路径，不作为本轮 report material 验证。
- 本轮已补 `macro_analysis` sandbox wrapper：不改宏观周期规则、不改 nowcast 阈值、不新增
  LLM，只把已有货币/信用/增长/通胀信号、投资时钟阶段、大类排序、行业轮动台账和任泽平观点
  对账整理为 `macro_regime_bridge`、`macro_signal_table`、`asset_allocation_view`、
  `sector_rotation_summary`、`macro_data_window`、`zeping_crosscheck_context`、自然语言
  `research_points`、更厚 evidence 和 `quality`。该 agent 的 `/compute` 仍是确定性宏观
  nowcast/规则引擎；LLM 仅属于 `/invoke` 解析和语言润色。
- 已扩展 dev 主系统与 sandbox main 的 adapter public-safe allowlist，并补 report bundle
  单测，确认上述材料能进入 `provenance.domain_metrics/drivers/data_quality/research_points`
  以及 `agent_evidence_bundle_v1.l2_agent_outputs[]`，不是只停留在 agent 原始 JSON。

验证（未调用 live HTTP endpoint）：

- `risk_crash` 新增代码与测试通过 `py_compile`。
- `risk_crash` 新增纯本地 report-material 单测 2 passed；直接 compute-core 验证 2.59s
  完成，返回 `agent_conclusion_v1`，包含 5 条 `research_points` 和预期 driver：
  `risk_score_bridge`、`feature_driver_table`、`history_context`、
  `model_validation_context`、`data_boundary`。
- `risk_crash` 直接 endpoint handler 验证 3.48s 完成，不经过 TestClient/HTTP；fixed DAG
  `gate_member` 投影后仍保留 5 条 `research_points`。
- `risk_crash` 模型版本边界精化后，focused tests 通过：
  `test_report_material.py` + `test_asof_leakage_probe.py` 返回 8 passed。
  直接 handler 验证：`600519.SH`、`as_of=2024-12-31` 返回
  `risk_score=0.2079`、`confidence=0.62`、`feature_data_anti_lookahead_passed=true`、
  `model_vintage_caveat=true`；主系统 adapter 映射后
  `provenance.domain_metrics.model_vintage_boundary` 和
  `provenance.drivers[].name=model_vintage_boundary` 均保留。
- 主系统 adapter focused tests 已在 dev 与 sandbox main 各跑一次：dev 35 passed，
  sandbox main 38 passed。
- `value_research_synthesis` 现有 report-material 测试通过：
  `test_domain_payload_adds_analyst_report_material_without_llm`。该测试使用本地 fixture
  验证材料投影，不作为 live 数据证据。
- `macro_index_valuation` 直接 `compute_core()` + `_attach_domain_payload()` 验证 0.70s 完成，
  返回 `agent_conclusion_v1`，包含 4 条 `research_points` 和预期 driver：
  `macro_direction_bridge`、`percentile_context`、`forward_return_context`、
  `data_window`、`method_assumptions`、`confidence_factors`。
- `macro_index_valuation` 直接 endpoint handler 验证 0.73s 完成，不经过 TestClient/HTTP，
  保留同样的 report material。
- 完整 TestClient endpoint 契约测试本轮未全部完成：直接 compute-core 很快，但本地
  Python 3.14 的 Starlette/httpx/anyio TestClient portal 路径会在 in-process 请求处卡住。
  这些中断尝试不作为验证证据；后续可单独升级/固定 TestClient 依赖后再跑 endpoint 契约全套。
- 额外注意：一次 `value_research_synthesis` 默认当前日期的直接 handler smoke 命中了
  `research_points`，但内部实时行情 lookup 被沙箱网络权限拦截并返回 warning；该 smoke
  不计为 live 证据。
- 已运行 P0 direct sandbox bundle check，artifact：
  `/tmp/lma-p0-direct-bundle-check/20260618T070209Z`。
  - 边界：direct sandbox handlers/core；不走 live HTTP、不走 TestClient、不调用 `/invoke`。
  - 标的：`value_research_synthesis` 与 `risk_crash` 使用 `600519.SH`、`as_of=2026-06-17`；
    `macro_index_valuation` 使用同日 `000300.SH` 作为宏观背景。
  - 三个 P0 L2 均经主系统 adapter 映射为 `conclusion_object_v1`，无 adapter failure。
  - `report_input_bundle_v1` 校验通过，`report_result_v1` 校验通过。
  - `agent_evidence_bundle_v1.l2_agent_outputs[]` 中：
    `value_research_synthesis` 有 3 条 `research_points`、6 条 evidence；
    `risk_crash` 有 5 条 `research_points`、6 条 evidence；
    `macro_index_valuation` 有 4 条 `research_points`、6 条 evidence。
  - fallback 中文报告已展开三家 P0 L2 的 `研究判断`、关键证据、核心指标、驱动因素和数据质量。
- 主系统固定 DAG 材料/报告相关单测已通过：
  `PYTHONPATH=src pytest -q tests/unit_tests/test_fixed_dag_external_adapter.py tests/unit_tests/test_fixed_dag_contracts.py tests/unit_tests/test_fixed_dag_executor.py tests/unit_tests/test_fixed_dag_report_synthesizer.py tests/unit_tests/test_fixed_dag_l3_explanation_synthesizer.py`
  返回 104 passed。
- `risk_identification` 新增代码与测试通过 `py_compile`。
- `risk_identification` 新增纯本地 report-material 单测 2 passed；测试直接调用
  `MarketRiskCore.compute()` 和 app 层 fixed-DAG 投影，不走 HTTP、不走 TestClient 请求。
- `value_ml_valuation` 现有 sandbox 测试通过：`py_compile`，以及
  `test_domain_contract_v1.py`、`test_service_contract.py`、`test_v21_compliance.py`
  共 22 passed。
- `value_meta_valuation` 现有 sandbox 测试通过：`py_compile`，以及
  `test_domain_contract_v1.py`、`test_v21_compliance.py` 共 8 passed。
- `risk_financial_fraud` 新增纯本地 report-material 单测 2 passed；测试直接调用
  `compute_core()` 和 app 层 fixed-DAG `gate_member` 投影，不走 HTTP、不调用 `/invoke`、
  不调用 provider。验证其输出包含 `fraud_risk_bridge`、`model_context`、
  `feature_diagnostics`、`risk_gate_rule`、至少 3 条 `research_points` 和多条 evidence。
- `macro_analysis` 新增纯本地 report-material 单测 2 passed；测试直接调用
  `compute_core()`，覆盖 data-nowcast 信号表和显式 scenario 路径，不走 HTTP、不调用
  `/invoke`、不调用 provider。验证其输出包含 `macro_regime_bridge`、
  `macro_signal_table`、`asset_allocation_view`、`sector_rotation_summary`、至少 4 条
  `research_points` 和多条 evidence，且 `validate_tool_result()` 仍通过。
- dev 主系统 adapter focused tests 通过：`test_fixed_dag_external_adapter.py` 返回
  36 passed；`test_fixed_dag_contracts.py` 返回 36 passed。
- sandbox main 同步 adapter/report bundle focused tests 通过：
  `test_fixed_dag_external_adapter.py` + `test_fixed_dag_contracts.py` 返回 76 passed。
- 已做一次 direct sandbox material check：直接调用 `risk_financial_fraud` 的 app 层
  `compute_payload()` 和 `macro_analysis.compute_core()`，再经 sandbox main adapter 构造
  `report_input_bundle_v1` 与 fallback `report_result_v1`。边界：不走 live HTTP、不走
  `/invoke`、不调用 provider。
  - `risk_financial_fraud` 映射校验通过，`l2_agent_outputs` 中 status=complete，
    4 条 `research_points`、6 个 driver、5 条 evidence；首条研究判断为
    `600519 财务造假风险闸门为 pass，风险分数 20/100。`
  - `macro_analysis` 映射校验通过，`l2_agent_outputs` 中 status=complete，
    5 条 `research_points`、10 个 driver、8 条 evidence；首条研究判断为
    `宏观周期判断为 宽货币·宽信用 / 复苏。`
  - `report_input_bundle_v1` 与 fallback `report_result_v1` 均校验通过，fallback 报告文本
    能读到 `财务造假风险闸门` 与 `宏观周期判断`。
- `risk_identification` 直接 fixed-DAG handler 验证通过：`600519.SH`、
  `as_of=2024-12-31` 返回 `agent_id=risk_identification`、`role=gate_member`、
  `risk_score=0.0171`、5 条 `research_points`、7 个 driver、10 条 evidence。
- 同一 payload 经主系统 adapter 映射为 `conclusion_object_v1`，无 adapter failure；
  `provenance` 中保留 5 条 `research_points`、9 个 driver、10 条 evidence，
  public-safe `risk_score` 位于 provenance/domain metrics。
- `value_ml_valuation` / `value_meta_valuation` 顶层 stance 修复后重跑既有 sandbox
  测试仍通过：ML 估值 22 passed，元学习估值 8 passed。
- 顶层 stance 修复后，两者经主系统 adapter 验证通过：
  `value_ml_valuation` 为 partial，保留 4 条 `research_points`、6 个 driver、7 条
  evidence；`value_meta_valuation` 为 complete，保留 3 条 `research_points`、
  6 个 driver、6 条 evidence。
- 已运行 6-agent direct sandbox bundle check，artifact：
  `/tmp/lma-6agent-direct-bundle-check/20260618T073557Z`。
  - 边界：direct sandbox handlers/core；不走 live HTTP、不走 TestClient、不调用 `/invoke`。
  - 纳入 agent：`value_research_synthesis`、`value_ml_valuation`、
    `value_meta_valuation`、`risk_crash`、`risk_identification`、
    `macro_index_valuation`。
  - `report_input_bundle_v1` 校验通过，`report_result_v1` 校验通过。
  - bundle quality summary：6 个 L2；5 complete，1 partial，0 error，
    0 个 without readable evidence。
  - fallback 中文报告长度 11138 字符；已展开 6 个 L2 的 `research_points`、
    关键证据、核心指标、驱动因素和数据质量。
  - `value_research_synthesis` 在 direct runner 中允许一次同进程重试，因为首次冷启动
    可能触发该服务自己的 6 秒超时降级。
- 已用该 6-agent `report_input_bundle_v1` 跑 default-off 真实 LLM report synthesis，
  artifact：`/tmp/lma-6agent-real-llm-report-synthesis/20260618T075202Z`。
  - 模型路径：`deepseek/deepseek-chat`。
  - 边界：只调用主系统报告综合器；不走 live agent HTTP、不走 TestClient、不调用
    `/invoke`；不存 provider raw response。
  - 结果：`attempted=true`、`provider_invoked=true`、`used_llm_report=true`、
    `report_result_v1` 校验通过，`status=complete`。
  - 产出：标题为“贵州茅台（600519.SH）多维度研判报告（基于 2024-12-31 时点数据）”，
    5 个 sections、7 个 evidence cards、7 条 limitations。
  - 内容质量：LLM 已能基于 6 个 L2 材料解释估值三方冲突、风险低但估值高、宏观中性、
    数据时点差异和 L3 缺失影响，明显优于 fallback 字段展开报告。
  - 注意：provider 栈的可选 tracing export 返回 403；报告综合本身完成并通过校验。

仍未做：

- 未修改 prod 服务副本。
- 未调用 live HTTP `/health`、`/compute`、`/invoke`。
- 未修改 runtime binding 或 live flags。
- 未把 placeholder agent 纳入真实 evidence。
- 尚未跑 live HTTP controlled sandbox demo；需要先解决或绕开本地 TestClient 兼容问题。

暂不处理：

- `market_fund_manager_behavior`
- `market_ipo_investor_behavior`
- `macro_sentiment`
- `macro_industry_hotspot`

这些 placeholder 或未完成 agent 需要服务 owner 完成真实能力后再纳入。

### Phase 6：真实 LLM 报告验证

等 report bundle 里已有足够 agent 研究材料后，再配置真实 provider 做 default-off
LLM report synthesis 验证。

当前进度（2026-06-18）：

- 已使用配置的 `openai/deepseek-v4-flash` 做一次受控验证，artifact：
  `/tmp/lma-r8-13n-l3-real-provider-demo/20260618T061151Z`。
- L3 解释层和最终报告 synthesis 都成功使用真实 provider。
- 最终报告不是 raw provider response；仓库只记录 sanitized 验证事实和 artifact 路径。

边界：

- 不存 raw provider response。
- 不输出密钥。
- 不改 runtime binding。
- provider 缺配置必须回退，并记录 secret-free 诊断。

### Phase 7：dev 主系统 L4 回填

当前进度（2026-06-19）：

- 已在 dev 主系统补齐 L4 compute-only handoff：
  `decision_synthesizer -> decision_result_v1`，
  `report_generator -> report_result_v1`。
- 这次只改 `langgraph-my-agent` 主系统：adapter、default-off compute bridge、
  executor、public mapping、contracts、smoke runner、测试和文档。
- L4 请求只在 `ENABLE_EXTERNAL_COMPUTE_DEMO=1` 且显式 allowlist 包含 L4 id
  时发生；仍然只走 `/v1/agent/compute`，不走 `/v1/agent/invoke`。
- `report_generator` 如果通过外部 compute 映射成功，主系统不会再用内部
  LLM report synthesis 覆盖该报告。
- `report_result_v1.sections`、`evidence_cards`、`limitations` 已可进入
  `final_emit_payload`、`emitted_bundle` 和 public answer card；smoke artifact
  `final_report.md` 改为渲染完整结构化报告。
- 已跑离线 R8-13A smoke：
  `/tmp/lma-dev-l4-offline-smoke-20260619`。
  - 模式：fake external compute、fake report model、fake placeholder model。
  - 未调用 live endpoint。
  - 映射 8 个 allowlist agent。
  - `final_report.md` 成功生成，包含 7 个 sections、5 个 evidence cards、
    4 条 limitations。
  - 修复了 smoke fake bridge 与真实 bridge 的 L4 参数签名不一致问题，并为
    fake L4 `decision_result_v1` / `report_result_v1` 增加 public-safe fixture。

边界：

- 未改 `.env`。
- 未改 `config/fixed_dag/runtime_bindings.json`。
- 未设置 `live_verified=true` 或 `invoke_enabled_by_default=true`。
- 未调用 live HTTP `/health`、`/compute`、`/invoke`。
- L4 仍不是生产默认启用；还需要后续 controlled L4 service smoke 才能评估
  外部服务生产 readiness。
