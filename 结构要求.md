# 智能体接入协议与 LangGraph 工具封装规范（对齐当前实现 v0.1）

本文为同学接入本项目的统一接口说明，已与当前代码保持一致。

## 1. 整体目标
- 所有分析类智能体对外暴露统一接口（AgentInput / AgentOutput）。
- Router / Manager 仅依赖 agent_id、元数据与统一 schema，不依赖内部实现。
- 无论是单 Prompt、独立 LangGraph 子图，还是专用模型微调，均以 LangGraph Tool 形式接入。

## 2. 核心抽象
### Agent 的定位
- 抽象为“接受子任务 + 共享上下文，返回结构化分析结果”的 LangGraph 工具。
- 每个 Agent 有唯一 `agent_id`（当前内置：news / filing / data / ecc）。
- Router/Manager 只负责：是否调用该 agent_id、提供标准化输入、读取标准化输出。

### 统一输入 / 输出（逻辑口径）
**AgentInput（工具入参）**
- `question`: 全局用户问题（原始自然语言）
- `subtask`: Manager 分配的子任务描述（来自 manager_assign）
- `history`: 与该 Agent 相关的历史对话（可选）
- `shared_context`: 上游中间结论（如其他 Agent 摘要）
- `tools_config`: 可选工具配置（如 allow_search 等）

**AgentOutput（工具出参，可序列化为 JSON）**
- `analysis`: 对 subtask 的分析文本
- `key_points`: 要点列表
- `evidence`: 关键证据（数据点、新闻标题、URL 或内部引用 ID）
- `confidence`: 0~1 置信度

## 3. Agent 元数据（供 Router/管理员）
- `id` / `name` / `description`
- `capabilities`: 能力标签（如 ["news","macro","event-driven"]）
- `input_type`: 市场对象（股票、指数、行业、宏观主题等）
- `latency_level`: "fast" | "medium" | "slow"
- `cost_level`: "cheap" | "normal" | "expensive"
- `version`: 模型/Prompt 版本  
当前内置元数据在代码注册，后续可通过文件扫描扩展。

## 4. LangGraph 接入：必须暴露的 Tool
- 每个 Agent 必须提供一个可绑定到 LLM 的工具，命名：`agent_{agent_id}`。
- 当前内置工具：`agent_news`、`agent_filing`、`agent_data`、`agent_ecc`。

### 工具输入字段（与 AgentInput 对齐）
- `question`: str
- `subtask`: str
- `shared_context`: dict
- `history`: list[dict]（可选）
- `tools_config`: dict（可选，如是否允许 search）

### 工具输出字段（与 AgentOutput 对齐）
- `analysis`: str
- `key_points`: list[str]
- `evidence`: list[str]
- `confidence`: float

## 5. 统一搜索工具封装
- 工具名：`tavily_search`（已内置，读取统一 `TAVILY_API_KEY`）。
- 建议输入：`query`、`k`（默认 5）、`time_range`（可选）。
- 输出：`results: list[{title,url,snippet,source}]`。
- 要求：若需搜索，一律调用 `tavily_search`，并在 `evidence` 中注明使用的结果。

## 6. Agent 实现建议（给同学的 checklist）
1) 明确职责边界，写清擅长问题，便于 Router 路由。  
2) System Prompt：角色介绍 + 可用工具说明 + 输出格式（analysis/key_points/evidence/confidence）。  
3) 如需搜索，使用统一的 `tavily_search`，在 Prompt 中说明引用方式。  
4) 内部流程可单轮或 ReAct，但对外必须收敛为一次 AgentOutput。  
5) 暴露 LangGraph Tool：命名 `agent_{id}`，入/出参遵循 AgentInput/AgentOutput。  
6) 提供元数据文件（如 `agent_{id}.json`），填好 id/name/description/capabilities/version 等。

## 7. 总图（Router / Manager）当前要求
- Router：只输出 agent_id 列表；解析失败兜底为 ["news","data","ecc"]，确保 plan 非空。  
- Manager：组装 AgentInput（question、subtask、shared_context、history、tools_config），调用对应 `agent_{id}` 工具；将返回的 AgentOutput 写入 `analyst_results`（按 agent_id 存储结构化结果）。  
- 流程：`__start__ → router → manager_assign → agent_{id}_node 循环 → manager_summary → __end__`；summary 仅面向用户，不输出 AgentInput JSON。
